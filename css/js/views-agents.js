export function renderAgents(ctx) {
  ctx.contentEl.innerHTML = `
    <div class="panel" style="max-width: 920px;">
      <div class="panel-head">AI AGENTS</div>
      <div class="panel-body">
        <div class="muted">
          Coming soon: agent marketplace + strategy vaults + permissions.
          This page is intentionally clean and credible instead of “fake numbers.”
        </div>
      </div>
    </div>
  `;
}
